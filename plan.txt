
pixel plotting *
primitives, circle, rectangle *
image blitting, +scaling 
text renderering
keyboard callbacks & state map

	minifb for framebuffer access *
	stb_image for image loading * <-- Add different sampling options
	font8x8 for simple bitmap font *
	add multi threaded drawing? 
